% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stat-pipeline.R
\name{wass2s_run_basins_stat}
\alias{wass2s_run_basins_stat}
\title{Run the statistical method across multiple basins}
\usage{
wass2s_run_basins_stat(
  data_by_product,
  hybas_id = "HYBAS_ID",
  pred_pattern_by_product = NULL,
  topK = 3,
  final_fuser = "rf",
  basins = NULL,
  parallel = FALSE,
  workers = 4,
  quiet = TRUE,
  ...
)
}
\arguments{
\item{data_by_product}{Named list of data frames (one per product).}

\item{hybas_id}{Column name for basin IDs (default: `"HYBAS_ID"`).}

\item{pred_pattern_by_product}{Named character vector: product -> regex to
select predictor columns for that product.}

\item{topK}{Integer, number of top products to keep in the fusion (by KGE).}

\item{final_fuser}{Name of the meta-learner to use (subset of \code{SUPPORTED_FUSERS}).}

\item{basins}{Optional vector of basin IDs to process; default uses all found.}

\item{parallel}{Logical, run basins in parallel using `{furrr}`.}

\item{workers}{Integer, number of parallel workers when `parallel = TRUE`.}

\item{quiet}{Logical; if \code{FALSE}, emits informative messages.}

\item{...}{Additional arguments passed to \code{wass2s_run_basin_mods_stat}.}
}
\value{
Named list keyed by basin id, each element the result of
  `wass2s_run_basin_mods_stat()`.
}
\description{
Iterates over basin ids, fits PCR/Ridge/Lasso for each product,
fuses top-K products by KGE, then trains a meta-learner on model outputs.
Supports parallel execution per basin.
}
\examples{
\dontrun{
res <- wass2s_run_basins_stat(data_by_product, pred_pattern_by_product = c(SST_CMCC="^pt_"))
}
}
\seealso{
[wass2s_run_basin_mods_stat()], [wass2s_cons_mods_stat()]
}
