% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/maps-metrics.R
\name{plot_metric_maps}
\alias{plot_metric_maps}
\title{Faceted metric maps (continuous) for hydrological performance}
\usage{
plot_metric_maps(
  sf_basins,
  metrics_df,
  basin_col = "HYBAS_ID",
  metrics,
  limits = NULL,
  divergent_metrics = c("KGE", "NSE"),
  title = NULL,
  file = NULL,
  width = 10,
  height = 6
)
}
\arguments{
\item{sf_basins}{An \code{sf} object or a path to a vector file (read via \code{sf::st_read}).}

\item{metrics_df}{A data.frame with one row per basin, containing \code{basin_col}
and the metric columns to plot.}

\item{basin_col}{Name of the join key present in both inputs (default "HYBAS_ID").}

\item{metrics}{Character vector of metric column names to facet (e.g., c("KGE","NSE","RMSE")).}

\item{limits}{Optional named list of numeric length-2 vectors overriding color limits
per metric, e.g. \code{list(KGE=c(-0.5,1), NSE=c(-0.5,1), RMSE=c(0,800))}.}

\item{divergent_metrics}{Character vector of metric names to plot with a divergent palette
(e.g., c("KGE","NSE")). Others use a sequential palette.}

\item{title}{Optional figure title.}

\item{file}{Optional path to save with \code{ggplot2::ggsave()}.}

\item{width, height}{Size in inches when saving.}
}
\value{
A ggplot object (returned invisibly if saved).
}
\description{
Plot basin-level performance metrics (e.g., KGE, NSE, RMSE) on maps,
with one facet per metric. Handles both divergent metrics (can be negative)
and positive-only metrics. If \pkg{viridis} is available, it is used by default.
}
\keyword{internal}
