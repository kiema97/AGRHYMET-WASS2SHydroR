% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-skill-matrix.R
\name{wass2s_plot_skill_matrix}
\alias{wass2s_plot_skill_matrix}
\title{Plot a WASS2S skill matrix}
\usage{
wass2s_plot_skill_matrix(
  x,
  metrics = NULL,
  row_col = NULL,
  group_col = NULL,
  approach = NULL,
  title = "WASS2S Skill Matrix",
  file = NULL,
  width = NULL,
  height = NULL
)
}
\arguments{
\item{x}{A WASS2S result object, a \code{wass2s_run_report}, or a data frame.}

\item{metrics}{Character vector of metric columns to display. If \code{NULL},
common deterministic, probabilistic and diagnostic metrics are detected.}

\item{row_col}{Column used for matrix rows. If \code{NULL}, the function tries
\code{model}, \code{method}, \code{selected_fusion_method}, \code{id}, then
\code{approach}.}

\item{group_col}{Optional column used for faceting, for example
\code{approach}. If \code{NULL}, a useful grouping column is detected when
available.}

\item{approach}{Optional approach label passed to \code{wass2s_run_report()}
when \code{x} is a raw WASS2S result object.}

\item{title}{Optional plot title.}

\item{file}{Optional path passed to \code{ggplot2::ggsave()}.}

\item{width, height}{Plot size in inches when saving.}
}
\value{
A \code{ggplot2} object, invisibly when saved to \code{file}.
}
\description{
Creates a compact diagnostic matrix inspired by PyCPT's model-by-metric skill
plots. The function accepts a WASS2S forecast result, a
\code{wass2s_run_report}, or a data frame. Metrics are shown as columns and
models, methods, basins, or user-selected rows are shown as rows.
}
\details{
Because metrics such as KGE, RMSE and RPSS have different units and
directions, tile colors use a within-metric normalized score: green is better,
red is worse, and the original metric value is printed inside each tile.
}
\examples{
\dontrun{
report <- wass2s_run_report(res, approach = "ML")
wass2s_plot_skill_matrix(report)
}

}
