% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/maps-prob.R
\name{plot_prob_maps}
\alias{plot_prob_maps}
\title{Plot probability maps for below/normal/above classes (faceted)}
\usage{
plot_prob_maps(
  sf_basins,
  probs_df,
  basin_col = "HYBAS_ID",
  limits = c(0, 1),
  palette = c("viridis"),
  facet_labels = c(p_below = "Below", p_normal = "Normal", p_above = "Above"),
  title = NULL,
  file = NULL,
  width = 10,
  height = 6
)
}
\arguments{
\item{sf_basins}{sf object or path to a vector file (shapefile, gpkg, …).}

\item{probs_df}{data.frame with one row per basin and columns:
`basin_col`, `p_below`, `p_normal`, `p_above`.}

\item{basin_col}{name of the basin id column shared by both inputs.}

\item{limits}{numeric length-2 for the fill scale limits (default c(0,1)).}

\item{palette}{continuous palette for probabilities (e.g. "viridis", "magma").}

\item{facet_labels}{named character vector to rename facets, e.g.
c(p_below="Below", p_normal="Normal", p_above="Above").}

\item{title}{optional title.}

\item{file}{optional path to save the figure via ggsave (format inferred from extension).}

\item{width, height}{units in inches for saving (if `file` supplied).}
}
\value{
ggplot object (invisibly if saved).
}
\description{
Join a basin geometry and a table of class probabilities, then plot
faceted maps (one facet per class). Expects columns `p_below`, `p_normal`,
`p_above`. If your table has several years, filter beforehand.
}
\keyword{internal}
