# Supported meta-learners for final fusion (identifiers)

Character vector of model keys allowed in `meta_spec()` and
`meta_grid()`.

## Usage

``` r
SUPPORTED_FUSERS
```

## Format

Character vector.
