# Internal helper to split fusion data into training and testing sets

Internal helper to split fusion data into training and testing sets

## Usage

``` r
.wass2s_split_fusion_data(
  fused_models,
  prediction_years = NULL,
  date_col = "YYYY"
)
```
