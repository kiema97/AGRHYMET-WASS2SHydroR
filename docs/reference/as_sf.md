# Coerce to sf (path or sf object)

Coerce to sf (path or sf object)

## Usage

``` r
as_sf(x)
```

## Arguments

- x:

  Character path to a vector file readable by sf (shp/gpkg/geojson/...),
  or an sf object.

## Value

sf object
