# Build a Ridge workflow (glmnet)

Build a Ridge workflow (glmnet)

## Usage

``` r
wf_ridge(rec)
```

## Arguments

- rec:

  A \`recipes\` recipe.

## Value

A \`workflows\` workflow for ridge regression.
