# Build a PCR workflow (with PCA threshold tuning)

Build a PCR workflow (with PCA threshold tuning)

## Usage

``` r
wf_pcr(rec)
```

## Arguments

- rec:

  A \`recipes\` recipe.

## Value

A \`workflows\` workflow (\`lm\` model with PCA preprocessor).
